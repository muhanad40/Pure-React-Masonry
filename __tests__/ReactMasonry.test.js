import React from 'react';
import { shallow } from 'enzyme';

import ReactMasonry from '../src/ReactMasonry';
import { createSetupCreator } from './utils';

const setup = createSetupCreator(ReactMasonry, {});

describe('ReactMasonry', () => {
  it('should render child items', () => {
    
  });
});